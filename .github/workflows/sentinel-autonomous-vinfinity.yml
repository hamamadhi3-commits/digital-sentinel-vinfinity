name: Sentinel Autonomous Infinity Engine

on:
  workflow_dispatch:
  push:
    branches:
      - main
  schedule:
    - cron: "0 */6 * * *"   # 🔁 Runs automatically every 6 hours

jobs:
  sentinel-core:
    runs-on: ubuntu-latest
    steps:
      - name: 🧠 Initialize
        run: echo "🚀 Launching Digital Sentinel Autonomous Infinity Engine..."

      - name: 📥 Checkout repository
        uses: actions/checkout@v4

      - name: 🐍 Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: 📦 Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: ⚡ Run Digital Sentinel Infinity Engine
        run: python3 src/core/main_controller_v11_4_quantum.py
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}

      - name: 🧹 Cleanup
        if: always()
        run: echo "✅ Sentinel Infinity cycle completed successfully."
