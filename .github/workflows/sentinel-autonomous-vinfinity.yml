name: Sentinel Autonomous Infinity Engine

on:
  schedule:
    - cron: "0 */6 * * *"   # ⏱ هەر 6 کاژمێر جارێک خۆکارانە دەستپێدەکات
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  sentinel-core:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run Digital Sentinel Parallel Engine
        run: |
          echo "🚀 Starting Quantum Infinity Engine..."
          python src/core/parallel_engine.py
